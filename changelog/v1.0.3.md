# RxFHIR v1.0.3

## Fixed

- Fixed Coverage creation on public HAPI servers by reusing existing resources that match the submitted insurance identifier instead of sending a duplicate `POST`.
- Fixed Medication creation on public HAPI servers by reusing existing resources that match the submitted medication code and code system instead of sending a duplicate `POST`.
- Reduced duplicate resource failures during emergency Creator submissions when common mock or demo values already existed on the target FHIR server.
